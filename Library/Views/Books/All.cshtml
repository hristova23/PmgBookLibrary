@model IEnumerable<BookViewModel>

@{
    ViewBag.Title = "All Books";
}

<div class="text-center row">
    @foreach (var book in Model)
    {
        <div class="card col-4" style="width: 20rem; ">
            <img class="card-img-top" style="height: 18rem; display: block;"
             src="@book.ImageUrl" alt="Book Image">
            <div class="card-body">
                <h5 class="card-title mt-1">@book.Title</h5>
                <p class="mb-0">Publisher: @book.Publisher</p>
                <p>Category: @book.Category</p>
            </div>

            <form class="input-group-sm" asp-controller="Books" asp-action="Details" asp-route-bookId="@book.Id">
                <input type="submit" value="Details" class="fs-6 btn btn-success mb-3 p-2" />
            </form>
        </div>
    }
</div>